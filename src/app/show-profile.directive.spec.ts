import { ShowProfileDirective } from './show-profile.directive';

describe('ShowProfileDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowProfileDirective();
    expect(directive).toBeTruthy();
  });
});
